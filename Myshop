 home page.html
 <!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Home | MyShop</title>
  <link rel="stylesheet" href="home.css" type="text/css" />
</head>
<body>
  <!-- Header -->
  <header>
    <h1>🛍️ MyShop</h1>
    <nav>
      <a href="homes.html" class="active">Home</a>
      <a href="products.html">Products</a>
      <a href="login.html">Login</a>
      <a href="contact.html">Contact</a>
    </nav>
    <button id="theme-toggle">🌙</button>
  </header>

  <!-- Hero Section -->
  <section class="hero fade-in">
    <div class="hero-text">
      <h2>Welcome to <span>MyShop</span></h2>
      <p>Find the best deals on electronics, accessories, and more.  
      Shop smart, shop <b>MyShop</b> — your trusted online store!</p>
      <a href="product.html" class="btn">Shop Now</a>
    </div>
    <div class="hero-img">
      <img src="/naan muthalvan/images/online-shopping.avif" alt="Shopping Image" />
    </div>
  </section>

  <!-- Slider Section -->
  <section class="slider fade-in">
    <div class="slides">
      <img src="/naan mudhalvan/images/selfie.jpg" class="slide active" alt="Offer 1" />
      <img src="/naan muthalvan/images/Tripod3.webp" class="slide" alt="Offer 2" />
      <img src="/naan muthalvan/images/neckband.png" class="slide" alt="Offer 3" />
    </div>
    <button class="prev">❮</button>
    <button class="next">❯</button>
  </section>

  <!-- Featured Products -->
  <section class="featured fade-in">
    <h2>✨ Featured Products ✨</h2>
    <div class="product-grid">
      <div class="product-card">
        <img src="images/headphone.png" alt="Headphones" />
        <h4>Wireless Headphones</h4>
        <p>₹999</p>
        <a href="product.html" class="btn-small">View</a>
      </div>
      <div class="product-card">
        <img src="images/speaker.jpg" alt="Speaker" />
        <h4>Bluetooth Speaker</h4>
        <p>₹1500</p>
        <a href="product.html" class="btn-small">View</a>
      </div>
      <div class="product-card">
        <img src="images/smartwatch.webp" alt="Smart Watch" />
        <h4>Smart Watch</h4>
        <p>₹2000</p>
        <a href="product.html" class="btn-small">View</a>
      </div>
      <div class="product-card">
        <img src="images/earbuds.webp" alt="Earbuds" />
        <h4>Wireless Earbuds</h4>
        <p>₹2500</p>
        <a href="product.html" class="btn-small">View</a>
      </div>
    </div>
  </section>

  <!-- About Section -->
  <section class="about fade-in">
    <h2>About MyShop</h2>
    <p>We are a trusted online store offering quality products at affordable prices.  
       From the latest gadgets to everyday accessories, MyShop makes online shopping easy and reliable.  
       Our goal is to deliver excellence and satisfaction to every customer!</p>
  </section>

  <!-- Footer -->
  <footer id="contact">
    <div class="footer-container">
      <div class="footer-about">
        <h3>🛍️ MyShop</h3>
        <p>Your trusted online store for top-quality gadgets and accessories.  
        Shop smart, shop <b>MyShop</b>!</p>
      </div>

      <div class="footer-links">
        <h4>Quick Links</h4>
        <ul>
          <li><a href="home.html">Home</a></li>
          <li><a href="product.html">Products</a></li>
          <li><a href="#">Offers</a></li>
          <li><a href="#contact">Contact</a></li>
        </ul>
      </div>

      <div class="footer-contact">
        <h4>Contact Us</h4>
        <p>📞 +91 98765 43210</p>
        <p>📧 support@myshop.com</p>
        <p>🏢 Madurai, Tamil Nadu</p>
      </div>

      <div class="footer-newsletter">
        <h4>Subscribe</h4>
        <p>Get updates on new arrivals & offers!</p>
        <form id="subscribeForm">
          <input type="email" id="email" placeholder="Enter your email" required />
          <button type="submit">Subscribe</button>
        </form>
        <div class="footer-social">
          <a href="#"><img src="icons/facebook.svg" alt="Facebook" /></a>
          <a href="#"><img src="icons/insta.png" alt="Instagram" /></a>
          <a href="#"><img src="icons/twitter.webp" alt="Twitter" /></a>
        </div>
      </div>
    </div>
    <p class="footer-bottom">&copy; 2025 MyShop. All Rights Reserved.</p>
  </footer>

  <script>
    // ===== Theme Toggle =====
const themeToggle = document.getElementById('theme-toggle');
themeToggle.addEventListener('click', () => {
  document.body.classList.toggle('dark-theme');
  themeToggle.textContent = document.body.classList.contains('dark-theme') ? '☀️' : '🌙';
  localStorage.setItem('theme', document.body.classList.contains('dark-theme') ? 'dark' : 'light');
});

// Load saved theme
if (localStorage.getItem('theme') === 'dark') {
  document.body.classList.add('dark-theme');
  themeToggle.textContent = '☀️';
}

// ===== Sticky Header on Scroll =====
window.addEventListener('scroll', () => {
  const header = document.querySelector('header');
  if (window.scrollY > 50) header.classList.add('scrolled');
  else header.classList.remove('scrolled');
});

// ===== Slider =====
const slides = document.querySelectorAll('.slide');
const prevBtn = document.querySelector('.prev');
const nextBtn = document.querySelector('.next');
let currentSlide = 0;

function showSlide(index) {
  slides.forEach((slide) => slide.classList.remove('active'));
  slides[index].classList.add('active');
}

nextBtn.addEventListener('click', () => {
  currentSlide = (currentSlide + 1) % slides.length;
  showSlide(currentSlide);
});

prevBtn.addEventListener('click', () => {
  currentSlide = (currentSlide - 1 + slides.length) % slides.length;
  showSlide(currentSlide);
});

// Auto-slide
setInterval(() => {
  currentSlide = (currentSlide + 1) % slides.length;
  showSlide(currentSlide);
}, 5000);

// ===== Scroll Reveal Animation =====
const faders = document.querySelectorAll('.fade-in');
const appearOptions = { threshold: 0.2, rootMargin: "0px 0px -50px 0px" };
const appearOnScroll = new IntersectionObserver((entries, observer) => {
  entries.forEach(entry => {
    if (!entry.isIntersecting) return;
    entry.target.classList.add('visible');
    observer.unobserve(entry.target);
  });
}, appearOptions);
faders.forEach(fader => appearOnScroll.observe(fader));

// ===== Newsletter Form =====
const subscribeForm = document.getElementById('subscribeForm');
subscribeForm.addEventListener('submit', (e) => {
  e.preventDefault();
  const emailInput = document.getElementById('email');
  if (emailInput.value) {
    alert(`Thank you for subscribing, ${emailInput.value}!`);
    emailInput.value = '';
  }
});
  </script>
</body>
</html>
 body {
  margin: 0;
  font-family: Arial, sans-serif;
  background: #fff;
  color: #333;
  scroll-behavior: smooth;
}

header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 15px 30px;
  background: #3f51b5;
  color: #fff;
  position: sticky;
  top: 0;
  z-index: 1000;
  transition: background 0.3s, box-shadow 0.3s;
}

header.scrolled {
  background: #303f9f;
  box-shadow: 0 2px 8px rgba(0,0,0,0.2);
}

header h1 { margin: 0; font-size: 24px; }
header nav a {
  color: #fff;
  text-decoration: none;
  margin-left: 20px;
}
header nav a.active { font-weight: bold; }
header button {
  background: transparent;
  border: none;
  font-size: 20px;
  cursor: pointer;
  color: #fff;
}

/* =======================
   HERO SECTION
======================= */
.hero {
  display: flex;
  justify-content: space-around;
  align-items: center;
  flex-wrap: wrap;
  padding: 40px 20px;
  background: linear-gradient(135deg, #5c6bc0, #7986cb);
  color: white;
  text-align: left;
}
.hero-text { max-width: 500px; }
.hero-text h2 { font-size: 32px; margin-bottom: 15px; }
.hero-text span { color: #ffeb3b; }
.hero-text p { font-size: 16px; margin-bottom: 20px; line-height: 1.5; }
.hero .btn {
  background: #ff9800;
  color: white;
  padding: 10px 18px;
  border-radius: 8px;
  text-decoration: none;
  font-weight: bold;
  transition: transform 0.3s, background 0.3s;
}
.hero .btn:hover { background: #ff5722; transform: translateY(-3px); }
.hero-img img { width: 350px; border-radius: 10px; box-shadow: 0 3px 10px rgba(0,0,0,0.3); }

/* =======================
   SLIDER
======================= */
.slider {
  position: relative;
  max-width: 500px;
  margin: 40px auto;
}
.slides img { display: none;  border-radius: 10px; }
.slides img.active { display: block; }
.prev, .next {
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
  background: rgba(0,0,0,0.5);
  color: #fff;
  border: none;
  font-size: 24px;
  padding: 5px 12px;
  cursor: pointer;
  border-radius: 5px;
}
.prev { left: 10px; }
.next { right: 10px; }

/* =======================
   FEATURED PRODUCTS
======================= */
.featured { text-align: center; padding: 40px 20px; }
.featured h2 { margin-bottom: 25px; color: #3f51b5; }
.product-grid {
  display: flex;
  justify-content: center;
  flex-wrap: wrap;
  gap: 20px;
}
.product-card {
  background: white;
  width: 200px;
  border-radius: 10px;
  padding: 15px;
  text-align: center;
  box-shadow: 0 2px 6px rgba(0,0,0,0.1);
  transition: transform 0.3s;
}
.product-card:hover { transform: translateY(-5px); }
.product-card img { width: 100%; height: 160px; object-fit: contain; }
.btn-small {
  display: inline-block;
  background: #ff9800;
  color: white;
  padding: 6px 12px;
  border-radius: 5px;
  margin-top: 8px;
  text-decoration: none;
  font-weight: bold;
  transition: transform 0.3s, background 0.3s;
}
.btn-small:hover { transform: translateY(-3px); background: #ff5722; }

/* =======================
   ABOUT SECTION
======================= */
.about {
  background: #f0f0f0;
  padding: 40px 20px;
  text-align: center;
}
.about h2 { margin-bottom: 15px; color: #3f51b5; }
.about p { max-width: 800px; margin: auto; font-size: 16px; line-height: 1.6; color: #444; }

/* =======================
   FOOTER
======================= */
footer { background: #3f51b5; color: #fff; padding: 40px 20px 20px; }
.footer-container { display: flex; flex-wrap: wrap; gap: 20px; justify-content: space-between; }
.footer-container div { flex: 1 1 200px; }
.footer-links ul { list-style: none; padding: 0; }
.footer-links ul li { margin-bottom: 8px; }
.footer-links ul li a { color: #fff; text-decoration: none; }
.footer-links ul li a:hover { text-decoration: underline; }
.footer-newsletter input {
  padding: 8px; border-radius: 5px; border: none; margin-right: 5px;
}
.footer-newsletter button {
  padding: 8px 12px; border: none; border-radius: 5px; background: #ff9800; color: #fff; cursor: pointer;
}
.footer-newsletter button:hover { background: #ff5722; }
.footer-social a img { width: 24px; margin-right: 8px; }
.footer-bottom { text-align: center; margin-top: 20px; font-size: 14px; }
.footer-social{
  display: flex;
  gap:20px;
  margin-top: 10px;
}
/* =======================
   DARK THEME
======================= */
.dark-theme {
  background: #121212;
  color: #fff;
}
.dark-theme header,
.dark-theme footer,
.dark-theme .product-card,
.dark-theme .hero {
  background: #1e1e1e;
  color: #fff;
}
.dark-theme a { color: #ffeb3b; }
.dark-theme .btn,
.dark-theme .btn-small,
.dark-theme .footer-newsletter button { background: #ff9800; color: #fff; }

/* =======================
   FADE-IN ANIMATION
======================= */
.fade-in {
  opacity: 0;
  transform: translateY(20px);
  transition: opacity 0.8s ease-out, transform 0.8s ease-out;
}
.fade-in.visible { opacity: 1; transform: translateY(0); }

.slides img{
      width: 400px;
      height:400px;
}
/* =======================
   RESPONSIVE
======================= */
@media (max-width: 768px) {
  .hero { flex-direction: column; text-align: center; }
  .hero-img img { width: 80%; margin-top: 20px; }
  .product-grid { flex-direction: column; align-items: center; }
  .footer-container { flex-direction: column; text-align: center; }
  .prev, .next { top: 90%; }
} 
product
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>🛍️ MyShop - Products</title>
  <link rel="stylesheet" href="product.css" />
</head>
<body>

  <!-- Header -->
  <header>
    <div class="header-left">
      <h1>🛍️ MyShop</h1>

      <div class="header-filters">
        <input type="text" id="search" placeholder="Search products..." onkeyup="filterProducts()" />

        <select id="category-filter" onchange="filterProducts()">
          <option value="all">All Categories</option>
          <option value="audio">Audio</option>
          <option value="gadgets">Gadgets</option>
          <option value="accessories">Accessories</option>
        </select>

        <select id="price-filter" onchange="filterProducts()">
          <option value="all">All Prices</option>
          <option value="0-1000">Below ₹1000</option>
          <option value="1000-2000">₹1000 - ₹2000</option>
          <option value="2000-5000">Above ₹2000</option>
        </select>
      </div>
    </div>

    <nav>
        <a href="/naan muthalvan/homes.html">Home</a>
      <a href="#" onclick="showProducts()">Products</a>
      <a href="#" onclick="showWishlist()">Wishlist (<span id="wishlist-count">0</span>)</a>
      <a href="#" onclick="showCart()">Cart (<span id="cart-count">0</span>)</a>
    </nav>
  </header>

  <!-- Main -->
  <main>
    <h2 id="page-title">Our Latest Products</h2>
    <div class="product-list" id="product-list"></div>
  </main>

  <!-- Footer -->
  <footer>
    <div class="footer-container">
      <div class="footer-section">
        <h3>About MyShop</h3>
        <p>MyShop offers the latest gadgets, audio products, and smart accessories at unbeatable prices. Discover innovation with quality and style.</p>
      </div>

      <div class="footer-section">
        <h3>Quick Links</h3>
        <ul>
          <li><a href="#" onclick="showProducts()">Products</a></li>
          <li><a href="#" onclick="showWishlist()">Wishlist</a></li>
          <li><a href="#" onclick="showCart()">Cart</a></li>
          <li><a href="#">Help & Support</a></li>
        </ul>
      </div>

      <div class="footer-section">
        <h3>Contact Us</h3>
        <p>📍 123 Market Street, Mumbai, India</p>
        <p>📞 +91 98765 43210</p>
        <p>📧 support@myshop.com</p>
      </div>

      <div class="footer-section">
        <h3>Follow Us</h3>
        <div class="social-links">
          <a href="#">🌐</a>
          <a href="#">📸</a>
          <a href="#">🐦</a>
          <a href="#">▶️</a>
        </div>
      </div>
    </div>
    <div class="footer-bottom">
      <p>&copy; 2025 MyShop. All rights reserved. | Designed with ❤️ by MyShop Team</p>
    </div>
  </footer>

  <script>
    const products = [
      { name: "Wireless Headphones", price: 999, image: "images/headphone.png", category: "audio", rating: 4 },
      { name: "Bluetooth Speaker", price: 799, image: "images/speaker.jpg", category: "audio", rating: 5 },
      { name: "Smart Watch", price: 1499, image: "images/smartwatch.webp", category: "gadgets", rating: 4 },
      { name: "Wireless Earbuds", price: 899, image: "images/earbuds.webp", category: "audio", rating: 3 },
      { name: "Tripod Stand", price: 2000, image: "images/tripod.jpg", category: "accessories", rating: 4 },
      { name: "Gaming Mouse", price: 2999, image: "images/mouse.avif", category: "accessories", rating: 5 },
      { name: "Laptop Cooling Pad", price: 1499, image: "images/pad.jpg", category: "accessories", rating: 4 },
      { name: "Power Bank", price: 1589, image: "images/power bank.jpg", category: "gadgets", rating: 4 },
      { name: "Wireless Keyboard", price: 5999, image: "images/keyboard.jpg", category: "accessories", rating: 5 }

    ];

    let cart = JSON.parse(localStorage.getItem("cart")) || [];
    let wishlist = JSON.parse(localStorage.getItem("wishlist")) || [];
    let ratings = JSON.parse(localStorage.getItem("ratings")) || {};

    document.addEventListener("DOMContentLoaded", () => {
      displayProducts(products);
      updateCounts();
    });

    // ⭐ Create star rating
    function createStars(productName, rating) {
      const starsDiv = document.createElement("div");
      starsDiv.className = "stars";
      for (let i = 1; i <= 5; i++) {
        const star = document.createElement("span");
        star.textContent = i <= rating ? "⭐" : "☆";
        star.onclick = () => rateProduct(productName, i);
        starsDiv.appendChild(star);
      }
      return starsDiv;
    }

    // ⭐ Save rating
    function rateProduct(name, rating) {
      ratings[name] = rating;
      localStorage.setItem("ratings", JSON.stringify(ratings));
      displayProducts(products);
    }

    // 🛍️ Display Products
    function displayProducts(list) {
      const container = document.getElementById("product-list");
      container.innerHTML = "";
      document.getElementById("page-title").textContent = "Our Latest Products";

      list.forEach((p) => {
        const isWished = wishlist.some((w) => w.name === p.name);
        const userRating = ratings[p.name] || p.rating;
        const card = document.createElement("div");
        card.className = "product-card";
        card.innerHTML = `
          <img src="${p.image}" alt="${p.name}">
          <h3>${p.name}</h3>
          <p>₹${p.price}</p>
          <button onclick="addToCart('${p.name}')">Add to Cart</button>
          <button onclick="toggleWishlist('${p.name}')">${isWished ? "💔 Remove Wishlist" : "❤️ Add Wishlist"}</button>
        `;
        const starsDiv = createStars(p.name, userRating);
        card.insertBefore(starsDiv, card.querySelector("p"));
        container.appendChild(card);
      });
    }

    // 🛒 Cart functions
    function addToCart(name) {
      const item = products.find((p) => p.name === name);
      cart.push(item);
      localStorage.setItem("cart", JSON.stringify(cart));
      updateCounts();
    }

    function removeFromCart(index) {
      cart.splice(index, 1);
      localStorage.setItem("cart", JSON.stringify(cart));
      updateCounts();
      showCart();
    }

    // ❤️ Wishlist
    function toggleWishlist(name) {
      const item = products.find((p) => p.name === name);
      const index = wishlist.findIndex((w) => w.name === name);
      if (index !== -1) wishlist.splice(index, 1);
      else wishlist.push(item);
      localStorage.setItem("wishlist", JSON.stringify(wishlist));
      updateCounts();
      displayProducts(products);
    }

    // 🔍 Filters
    function filterProducts() {
      const search = document.getElementById("search").value.toLowerCase();
      const category = document.getElementById("category-filter").value;
      const price = document.getElementById("price-filter").value;
      let filtered = products.filter(
        (p) =>
          p.name.toLowerCase().includes(search) &&
          (category === "all" || p.category === category)
      );
      if (price !== "all") {
        const [min, max] = price.split("-").map(Number);
        filtered = filtered.filter((p) => (max ? p.price >= min && p.price <= max : p.price > min));
      }
      displayProducts(filtered);
    }

    // 🧮 Update Counts
    function updateCounts() {
      document.getElementById("cart-count").textContent = cart.length;
      document.getElementById("wishlist-count").textContent = wishlist.length;
    }

    // ❤️ Wishlist Page
    function showWishlist() {
      const container = document.getElementById("product-list");
      const title = document.getElementById("page-title");
      title.textContent = "Your Wishlist ❤️";
      container.innerHTML = "";
      if (wishlist.length === 0) {
        container.innerHTML = '<p class="empty-msg">Your wishlist is empty.</p>';
        return;
      }
      wishlist.forEach((p) => {
        const div = document.createElement("div");
        div.className = "product-card";
        const userRating = ratings[p.name] || p.rating;
        div.innerHTML = `
          <img src="${p.image}" alt="${p.name}">
          <h3>${p.name}</h3>
          <p>₹${p.price}</p>
          <button onclick="addToCart('${p.name}')">Add to Cart</button>
          <button onclick="toggleWishlist('${p.name}')">💔 Remove</button>
        `;
        const starsDiv = createStars(p.name, userRating);
        div.insertBefore(starsDiv, div.querySelector("p"));
        container.appendChild(div);
      });
    }

    // 🛒 Cart Page
    function showCart() {
      const container = document.getElementById("product-list");
      const title = document.getElementById("page-title");
      title.textContent = "Your Shopping Cart 🛒";
      container.innerHTML = "";
      if (cart.length === 0) {
        container.innerHTML = '<p class="empty-msg">Your cart is empty.</p>';
        return;
      }
      let total = 0;
      cart.forEach((p, i) => {
        total += p.price;
        const div = document.createElement("div");
        div.className = "product-card";
        const userRating = ratings[p.name] || p.rating;
        div.innerHTML = `
          <img src="${p.image}" alt="${p.name}">
          <h3>${p.name}</h3>
          <p>₹${p.price}</p>
          <button onclick="removeFromCart(${i})">🗑️ Remove</button>
        `;
        const starsDiv = createStars(p.name, userRating);
        div.insertBefore(starsDiv, div.querySelector("p"));
        container.appendChild(div);
      });
      const totalDiv = document.createElement("div");
      totalDiv.className = "cart-total";
      totalDiv.innerHTML = `<h3>Total: ₹${total}</h3><button class="checkout-btn" onclick="checkout()">Checkout</button>`;
      container.appendChild(totalDiv);
    }

    // ✅ Checkout
    function checkout() {
      if (cart.length === 0) return;
      const container = document.getElementById("product-list");
      container.innerHTML = `
        <div class="checkout-message">
          <h2>✅ Thank you for your purchase!</h2>
          <p>Your order has been successfully placed.</p>
          <p>We’ll send you a confirmation email shortly.</p>
          <button onclick="showProducts()">🛍️ Continue Shopping</button>
        </div>
      `;
      cart = [];
      localStorage.setItem("cart", JSON.stringify(cart));
      updateCounts();
    }

    function showProducts() {
      document.getElementById("page-title").textContent = "Our Latest Products";
      displayProducts(products);
    }
  </script>
</body>
</html>
body {
  font-family: Arial, sans-serif;
  margin: 0;
  background: #f8f8f8;
}

/* Header */
header {
  background: #283593;
  color: white;
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 15px 20px;
}

header h1 {
  font-size: 22px;
}

nav a {
  color: white;
  text-decoration: none;
  margin-left: 15px;
}

nav a:hover {
  color: #ffb300;
}

/* Filters Section */
.filters {
  display: flex;
  justify-content: center;
  gap: 10px;
  margin: 15px;
}

.filters input,
.filters select {
  padding: 8px;
  border-radius: 5px;
  border: 1px solid #ccc;
}

/* Product List */
.product-list {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(230px, 1fr));
  gap: 20px;
  padding: 20px;
}

/* Product Card */
.product-card {
  background: white;
  border-radius: 10px;
  text-align: center;
  padding: 15px;
  box-shadow: 0 2px 5px rgba(0,0,0,0.1);
  transition: 0.3s;
}

.product-card img {
  width: 100%;
  height: 180px;
  object-fit: contain;
  border-radius: 10px;
}

/* Hover Effect */
.product-card:hover {
  transform: translateY(-5px);
  background-color: #1a237e; /* Deep navy */
  color: white; /* Text white on hover */
}

.product-card:hover button {
  background-color: #ffb300; /* Button color visible on hover */
  color: #1a237e; /* Button text contrasts */
}

/* Buttons */
button {
  padding: 8px 12px;
  border: none;
  background: #283593;
  color: white;
  border-radius: 5px;
  cursor: pointer;
  margin: 5px;
  transition: 0.3s;
}

button:hover {
  background: #ff9800;
  color: white;
}

/* Ratings Stars */
.rating {
  display: flex;
  justify-content: center;
  margin: 5px 0;
}

.rating span {
  font-size: 18px;
  cursor: pointer;
  color: #ccc;
  transition: 0.2s;
}

.rating span.hover,
.rating span.selected {
  color: #ffb300;
}

/* Footer */
footer {
  background: #283593;
  color: white;
  text-align: center;
  padding: 20px 10px;
  margin-top: 30px;
}

footer p {
  margin: 5px 0;
}

footer a {
  color: #ffb300;
  text-decoration: none;
}

footer a:hover {
  text-decoration: underline;
}

/* Empty Message */
.empty-msg {
  text-align: center;
  margin-top: 50px;
  font-size: 18px;
  color: gray;
}
 login
 <!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login | MyShop</title>
  <link rel="stylesheet" href="login.css" type="text/css">
</head>
<body class="login-page">
  <div class="login-container">
    <h1>🛍️ MyShop</h1>
    <p>Sign in to continue shopping</p>

    <form id="loginForm">
      <div class="form-group">
        <label for="username">Username</label>  
        <input type="text" id="username" placeholder="Enter Username" required>
      </div>    

      <div class="form-group">
        <label for="email">Email</label>  
        <input type="email" id="email" placeholder="Enter Email" required>
      </div>    

      <div class="form-group">
        <label for="phone">Phone Number</label>  
        <input type="tel" id="phone" placeholder="Enter Phone Number" required>
      </div>    

      <div class="form-group">
        <label for="password">Password</label>
        <input type="password" id="password" placeholder="Enter Password" required>
      </div>  

      <button type="submit">Login</button>
    </form>

    <p class="signup-text">Don’t have an account? <a href="#">Sign Up</a></p>
  </div>

  <script>
    document.getElementById("loginForm").addEventListener("submit", function(e) {
      e.preventDefault(); // Stop form from submitting

      // Get form fields
      const username = document.getElementById("username").value.trim();
      const email = document.getElementById("email").value.trim();
      const phone = document.getElementById("phone").value.trim();
      const password = document.getElementById("password").value.trim();

      // Error fields
      const usernameError = document.getElementById("usernameError");
      const emailError = document.getElementById("emailError");
      const phoneError = document.getElementById("phoneError");
      const passwordError = document.getElementById("passwordError");

      // Reset previous errors
      usernameError.textContent = "";
      emailError.textContent = "";
      phoneError.textContent = "";
      passwordError.textContent = "";

      let valid = true;

      // Username validation
      if (username === "") {
        usernameError.textContent = "Username is required.";
        valid = false;
      }

      // Email validation (basic regex)
      const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
      if (email === "") {
        emailError.textContent = "Email is required.";
        valid = false;
      } else if (!emailRegex.test(email)) {
        emailError.textContent = "Enter a valid email address.";
        valid = false;
      }

      // Phone validation (10 digits)
      const phoneRegex = /^[0-9]{10}$/;
      if (phone === "") {
        phoneError.textContent = "Phone number is required.";
        valid = false;
      } else if (!phoneRegex.test(phone)) {
        phoneError.textContent = "Enter a valid 10-digit phone number.";
        valid = false;
      }

      // Password validation
      if (password === "") {
        passwordError.textContent = "Password is required.";
        valid = false;
      } else if (password.length < 6) {
        passwordError.textContent = "Password must be at least 6 characters.";
        valid = false;
      }

      // Show alert based on validity
      if (valid) {
        alert("✅ Login successful! Welcome back, " + username + "!");
        // Redirect or further logic here:
        // window.location.href = "dashboard.html";
      } else {
        alert("⚠️ Please fix the highlighted errors and try again.");
      }
    });
  </script>
</body>
</html>
 body.login-page {
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
  background: linear-gradient(135deg, #3f51b5, #5c6bc0);
  font-family: 'Poppins', sans-serif;
  margin: 0;
}

/* ===== Login Container ===== */
.login-container {
  background: #ffffff;
  padding: 40px 35px;
  border-radius: 16px;
  box-shadow: 0 5px 25px rgba(0,0,0,0.2);
  text-align: center;
  width: 350px;
  animation: fadeIn 0.8s ease-in-out;
}

/* ===== Title & Subtitle ===== */
.login-container h1 {
  color: #3f51b5;
  font-size: 30px;
  margin-bottom: 8px;
}

.login-container p {
  color: #666;
  font-size: 15px;
  margin-bottom: 25px;
}

/* ===== Form Styling ===== */
.login-container form {
  display: flex;
  flex-direction: column;
  gap: 14px;
}

.form-group {
  text-align: left;
}

label {
  font-weight: 600;
  color: #333;
  margin-bottom: 6px;
  display: inline-block;
}

.login-container input {
  width: 90%;
  padding: 12px 14px;
  border-radius: 8px;
  border: 1px solid #ccc;
  font-size: 15px;
  transition: all 0.3s ease;
}

/* Focus effect */
.login-container input:focus {
  border-color: #3f51b5;
  outline: none;
  box-shadow: 0 0 6px rgba(63, 81, 181, 0.3);
}

/* Highlight invalid inputs */
.login-container input.invalid {
  border-color: #e53935;
  background-color: #fff5f5;
}

/* ===== Button ===== */
.login-container button {
  width: 100%;
  padding: 12px;
  background: #3f51b5;
  border: none;
  border-radius: 8px;
  color: white;
  font-size: 16px;
  font-weight: bold;
  margin-top: 5px;
  cursor: pointer;
  transition: background 0.3s ease, transform 0.2s;
}

.login-container button:hover {
  background: #303f9f;
  transform: translateY(-2px);
}

/* ===== Signup Text ===== */
.signup-text {
  font-size: 14px;
  color: #666;
  margin-top: 18px;
}

.signup-text a {
  color: #3f51b5;
  text-decoration: none;
  font-weight: bold;
  transition: color 0.3s ease;
}

.signup-text a:hover {
  text-decoration: underline;
  color: #283593;
}

/* ===== Error Messages ===== */
.error {
  color: #e53935;
  font-size: 13px;
  display: block;
  margin-top: 4px;
  min-height: 16px; /* keeps layout consistent even if no error */
}

/* ===== Animation ===== */
@keyframes fadeIn {
  from { opacity: 0; transform: translateY(15px); }
  to { opacity: 1; transform: translateY(0); }
}

/* ===== Responsive Design ===== */
@media (max-width: 480px) {
  .login-container {
    width: 85%;
    padding: 30px 20px;
  }
  .login-container h1 {
    font-size: 26px;
  }
} 
contact
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Contact Us | MyShop</title>
  <link rel="stylesheet" href="contact.css" type="text/css">
</head>
<body class="contact-page">

  <div class="contact-container">
    <h1>📞 Contact Us</h1>
    <p>We’d love to hear from you! Fill out the form below and we’ll get back to you soon.</p>

    <form id="contactForm">
      <div class="form-group">
        <label for="name">Full Name</label>
        <input type="text" id="name" placeholder="Enter your name" required>
      </div>

      <div class="form-group">
        <label for="email">Email Address</label>
        <input type="email" id="email" placeholder="Enter your email" required>
      </div>

      <div class="form-group">
        <label for="phone">Phone Number</label>
        <input type="tel" id="phone" placeholder="Enter your phone number" required>
      </div>

      <div class="form-group">
        <label for="message">Message</label>
        <textarea id="message" rows="4" placeholder="Write your message here..." required></textarea>
      </div>

      <button type="submit">Send Message</button>
    </form>
  </div>

  <script>
    document.getElementById("contactForm").addEventListener("submit", function(e) {
      e.preventDefault();

      // Get form fields
      const name = document.getElementById("name").value.trim();
      const email = document.getElementById("email").value.trim();
      const phone = document.getElementById("phone").value.trim();
      const message = document.getElementById("message").value.trim();

      // Error elements
      const nameError = document.getElementById("nameError");
      const emailError = document.getElementById("emailError");
      const phoneError = document.getElementById("phoneError");
      const messageError = document.getElementById("messageError");

      // Reset previous errors
      nameError.textContent = "";
      emailError.textContent = "";
      phoneError.textContent = "";
      messageError.textContent = "";

      let valid = true;

      // Name validation
      if (name === "") {
        nameError.textContent = "Name is required.";
        valid = false;
      }

      // Email validation
      const emailRegex = /^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/;
      if (email === "") {
        emailError.textContent = "Email is required.";
        valid = false;
      } else if (!emailRegex.test(email)) {
        emailError.textContent = "Enter a valid email address.";
        valid = false;
      }

      // Phone validation
      const phoneRegex = /^[0-9]{10}$/;
      if (phone === "") {
        phoneError.textContent = "Phone number is required.";
        valid = false;
      } else if (!phoneRegex.test(phone)) {
        phoneError.textContent = "Enter a valid 10-digit phone number.";
        valid = false;
      }

      // Message validation
      if (message === "") {
        messageError.textContent = "Message cannot be empty.";
        valid = false;
      }

      // Show alert
      if (valid) {
        alert("✅ Thank you, " + name + "! Your message has been sent successfully.");
        document.getElementById("contactForm").reset();
      } else {
        alert("⚠️ Please correct the errors and try again.");
      }
    });
  </script>

</body>
</html>
 body.contact-page {
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
  background: linear-gradient(135deg, #3f51b5, #5c6bc0);
  font-family: 'Poppins', sans-serif;
  margin: 0;
}

/* ===== Container ===== */
.contact-container {
  background: #fff;
  padding: 40px 35px;
  border-radius: 16px;
  box-shadow: 0 5px 20px rgba(0,0,0,0.2);
  text-align: center;
  width: 400px;
  animation: fadeIn 0.8s ease-in-out;
}

/* ===== Title & Subtitle ===== */
.contact-container h1 {
  color: #3f51b5;
  font-size: 28px;
  margin-bottom: 10px;
}

.contact-container p {
  color: #666;
  font-size: 15px;
  margin-bottom: 20px;
}

/* ===== Form ===== */
.contact-container form {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.form-group {
  text-align: left;
}

label {
  font-weight: 600;
  color: #333;
  margin-bottom: 6px;
  display: inline-block;
}

input, textarea {
  width: 90%;
  padding: 12px 14px;
  border-radius: 8px;
  border: 1px solid #ccc;
  font-size: 15px;
  transition: all 0.3s ease;
  resize: none;
}

input:focus, textarea:focus {
  border-color: #3f51b5;
  box-shadow: 0 0 5px rgba(63, 81, 181, 0.3);
  outline: none;
}

/* ===== Button ===== */
button {
  width: 100%;
  padding: 12px;
  background: #3f51b5;
  border: none;
  border-radius: 8px;
  color: white;
  font-size: 16px;
  font-weight: bold;
  margin-top: 10px;
  cursor: pointer;
  transition: background 0.3s ease, transform 0.2s;
}

button:hover {
  background: #303f9f;
  transform: translateY(-2px);
}

/* ===== Error Messages ===== */
.error {
  color: red;
  font-size: 13px;
  display: block;
  margin-top: 3px;
  min-height: 16px;
}

/* ===== Animation ===== */
@keyframes fadeIn {
  from { opacity: 0; transform: translateY(10px); }
  to { opacity: 1; transform: translateY(0); }
}

/* ===== Responsive ===== */
@media (max-width: 480px) {
  .contact-container {
    width: 85%;
    padding: 30px 20px;
  }
}
